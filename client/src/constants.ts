import { Point } from "pixi.js";

export const MAX_SIDE_WEB = 500
export const MAX_SIDE_ROS = 11
export const ROS_PORT = 9000
export const PIXI_CENTER_WEB = { x: MAX_SIDE_WEB / 2, y: MAX_SIDE_WEB / 2, theta: 0 };
export const PIXI_CENTER_WEB_POINT = new Point(MAX_SIDE_WEB / 2, MAX_SIDE_WEB / 2)
